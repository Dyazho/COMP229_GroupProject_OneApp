module.exports = 
{
    "URI": "mongodb://localhost/survey_list",
    /* will create an opject if we are going to convert to ANGULAR */
    //"Secret": "SomeSecert"
}

// npm install mongoose --save
/*


const MongoClient = require('mongodb').MongoClient;
const uri = "mongodb+srv://admin:<password>@survey.gsgfm.mongodb.net/<dbname>?retryWrites=true&w=majority";
const client = new MongoClient(uri, { useNewUrlParser: true });
client.connect(err => {
  const collection = client.db("test").collection("devices");
  // perform actions on the collection object
  client.close();
});

});
the password is: Wjn2FsisojpG2rVO

*/

/*
https://github.com/Munther-Al-Shukair/COMP229-F2020-Team-Progect.git
*/
/* private
https://github.com/Munther-Al-Shukair/COMP229-F2020-Team-Project-OneApp.git
*/